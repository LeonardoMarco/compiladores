class Pressao2:
		def bool podeEntregarDepois(bool dizQueSim):
			return (true == dizQueSim);
		end;

    defstatic void main(String[] args):
    	writeln("Caros alunos de Compiladores,");
		writeln("Este eh um codigo de exemplo em Pyscal!");
		writeln("TESTE DO LON");
		
		if(podeEntregarDepois(true)):
			writeln("Pode, mas nao tem ponto");
		end;
	end;
end.